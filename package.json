{
  "name": "playwrightautomation",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "npx playwright test tests/ParameterizedTestsPO.spec.js --headed",
    "regression": "npx playwright test",
    "webTests": "npx playwright test --grep @Web",
    "APITests": "npx playwright test --grep @API",
    "TestsInSafariConfig": "npx playwright test --config=playwright.config1.js --project=safari",
    "CucumberRun": "npx cucumber-js --tags @Regression --retry 1 --exit --format html:cucumber-report.html --parallel 2"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@playwright/test": "^1.36.0",
    "allure-playwright": "^2.4.0"
  },
  "dependencies": {
    "@cucumber/cucumber": "^9.3.0"
  }
}
